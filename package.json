{
  "name": "url-classifier-app",
  "version": "1.0.0",
  "description": "URL Classifier Flask Application",
  "scripts": {
    "dev": "python3 app.py",
    "install-python-deps": "python3 -m pip install -r requirements.txt"
  },
  "dependencies": {}
}
