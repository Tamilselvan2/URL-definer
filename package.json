{
  "name": "url-classifier-app",
  "version": "1.0.0",
  "description": "URL Classifier Flask Application",
  "scripts": {
    "setup": "python3 -m venv venv && ./venv/bin/pip install -r requirements.txt",
    "dev": "./venv/bin/python app.py",
    "install-python-deps": "./venv/bin/pip install -r requirements.txt"
  },
  "dependencies": {}
}
